package com.toufika.spring_boot_webapplication.service;

import org.springframework.stereotype.Component;

import com.toufika.spring_boot_webapplication.model.MyModel;


public class Myservice implements MyServiceInterface{

	@Override
	public void save(MyModel model1) {
		//logic written in the year 2000 and it is consider old logic and process time is 10 sec.
	}
}
